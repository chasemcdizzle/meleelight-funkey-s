# port/sim/device/m4-freeze-manifest.txt — M4 EXIT GATE freeze manifest
# (PROCESS §4 reviewed-pin rule; assembled iter 116, the m3 manifest's
# format and status vocabulary inherited verbatim).
#
# Record grammar (verify_m4.sh parses it anchored, fail-closed):
#   <sha256> <path> <status> <cite>
# status tokens:
#   reviewed-go      — surface closed a Tier-A arc at VERDICT: GO (or a
#                      driver-recorded CAPPED-CLOSED; cite = the arc's
#                      verdict log and/or the driver AGENT-LOG entry)
#   oracle-frozen    — HARD RULE 3 surface (never edited since freeze)
#   grandfathered-m2 — M2-era judge-path tool predating PROCESS
#                      adoption; proven by the M2 exit gate corpus
#   arc-in-flight / arc-pending — grammar-legal, but verify_m4.sh [0b]
#                      HARD-REFUSES them in AUTHORITATIVE mode. Rows
#                      carrying one are listed in the UNCLOSED ROWS
#                      block below; while any remains, an AUTHORITATIVE
#                      run REFUSES before leg 1.
#
# The producer set is EXACTLY verify_m4.sh's REQUIRED_PRODUCERS literal
# (44 paths). What is asserted is SET equality, not row order: the gate
# compares the two path sets sorted and space-normalized, which implies
# count AND identity without constraining the order rows appear in
# (pipeline/lib/tables-anim-xref.js, added iter-117, sits at
# verify_m4.sh:157 but last in this file — legal, and asserted as such).
# Corrected per .loop/review-117-delta-1.log [L]; the header previously
# said "43 paths, same order" and both halves were false.
# verify_m4.sh HARD-REFUSES to run any leg when
# a producer's current bytes mismatch its pin, when a row is missing or
# duplicated, or when an extra row appears ("hashes prove identity, not
# approval" — a producer change invalidates prior gate evidence).
#
# CITE DISCIPLINE (iter 116): every cite names evidence verified by
# reading the log's LAST anchored `^VERDICT: GO` line, or a driver
# CAPPED-CLOSED AGENT-LOG entry. Two measured traps are avoided by
# construction: `.loop/review-96-aplus-1.log`'s `**VERDICT: GO**` is
# bold markdown, NOT anchored-parseable, and is never cited; prompt-text
# occurrences of the verdict format are not verdicts (the reviewer's
# verdict is the LAST anchored line).
#
# BYTE COVERAGE (corrected in round 1, review-116-1 [M]): a cite must
# show the arc reviewed the producer's CURRENT bytes. TIMESTAMPS DO NOT
# SHOW THIS and are not used as the binding evidence — a review normally
# PRECEDES the commit it closes, so a cite log legitimately predates its
# producer's commit (measured: review-113-7.log 15:58:42 vs c587390
# 16:05:34; review-115-3.log 19:49:19 vs adf072a 20:08:19). What binds
# instead, per row, is the arc's own evidence packet naming the file:
#   check-device-fullgame.sh  -> .loop/review-113-7-diff.txt (sole file)
#   check-device-opk.sh, .desktop -> .loop/review-115-3-diff-cumulative.txt
#   verify_m4.sh (teeth=21)   -> .loop/review-109-10-diff.txt hunk
# plus, for unchanged-since-GO rows, the fact that the producer has had
# NO commit since the cited closure. The one row where NO evidence of
# either kind exists is judge-render-timing.js — see the next block.
#
# UPDATE PATH (reviewed change only): a producer edit lands through its
# review arc; the SAME commit re-pins it here by regenerating its line:
#   shasum -a 256 <path>   -> replace the record's sha field
# (iter-115's riglib.sh row went stale exactly because a commit touching
# a pinned producer did not re-pin in the same commit.)
#
# INTEGRITY ANCHOR: THIS FILE's own sha256 is pinned as the
# MANIFEST_SHA256= literal inside verify_m4.sh, verified BEFORE any row
# is trusted. UPDATE DISCIPLINE (binding): ANY edit to this file — any
# row, any comment byte — requires updating that literal in
# port/sim/device/verify_m4.sh IN THE SAME COMMIT:
#   shasum -a 256 port/sim/device/m4-freeze-manifest.txt
# NORMALIZED SELF-REFERENCE: the verify_m4.sh row below pins sha256 of
# that file's bytes EXCLUDING its single ^MANIFEST_SHA256= line
# (compute: grep -v '^MANIFEST_SHA256=' port/sim/device/verify_m4.sh |
# shasum -a 256) — a full-byte self-row plus the anchor would be a
# two-unknown fixed point with no solution; the excluded line is
# protected by the anchor equality itself.
#
# UNCLOSED ROWS — five producers are unresolved: the judge
# (port/gfx/judge-render-timing.js, `arc-pending`) plus FOUR
# `arc-in-flight` — the three evidence engines check-device-fullgame.sh,
# check-device-target.sh and check-device-foh.sh (iter-117 jrt
# producer-pin fan-out changed their bytes) and verify_m4.sh itself.
# (Count corrected per .loop/review-117-jrt-7.log [L3].) This makes verify_m4.sh
# [0b] HARD-REFUSE in AUTHORITATIVE mode. verify_m4.sh is unresolved
# because THIS iteration's driver-sanctioned universe extension changed
# its bytes and its delta arc has not reached GO
# (.loop/review-116-2o.log, VERDICT: NO-GO); carrying `reviewed-go`
# there would be inherited approval applied to later bytes — the exact
# class round 1 raised as [H] against judge-render-timing.js. This is the
# truthful status and the refusal is a correct outcome, not a defect.
# Each was given a Tier A+ arc over its CURRENT bytes (PROCESS §3:
# judge/verify paths take an independent second reviewer as well). Both
# arcs RAN in iter 116 and both returned NO-GO with Medium+ findings, so
# no row flips. A row flips to `reviewed-go` only when its findings are
# fixed and BOTH reviewers reach an anchored VERDICT: GO — never by
# re-citing an older log, and never by driver-capping an arc that has
# not converged.
#   port/gfx/judge-render-timing.js
#     -> .loop/review-116-jrt-1.log (codex) VERDICT: NO-GO — two [M]:
#        leading-zero numeric tokens accepted (grammar, PROCESS §3
#        resembles-but-doesn't-match) and an unchecked 3-term integer
#        sum that can leave full_p99_ns inexact while still emitting
#        judge_complete=1. The arc's positive results are recorded too:
#        every vacuous-pass class fails closed, percentiles matched an
#        independent BigInt oracle over all 61 real artifacts, and the
#        Tier A+ byte-identity regression PASSED (current bytes
#        reproduced all 29 archived judgments byte-for-byte). The file's
#        bytes are driver-frozen for iter 116, so the two [M] fixes are
#        a DRIVER decision, not a writer one.
#     -> .loop/review-116-jrt-2o.log (fresh Opus 5, the independent
#        second under the PROCESS §11 owner ruling of 2026-07-26 — grok
#        aborted three times without emitting a verdict, see
#        .loop/review-116-jrt-2-grok-aborted*.log) VERDICT: NO-GO, and
#        it found the STRONGEST defect against this row, worse than
#        either codex [M]: a DEMONSTRATED FALSE GREEN on the gate's own
#        headline PERF number. judge-render-timing.js:74 hard-fails a
#        row whose render column is 0 (a zero-cost render is a recording
#        bug) but NO equivalent guard exists for the sim column, which
#        :79 (full[i] = s + r + p) consumes as the dominant term of the
#        gated value. Measured: zeroing column 1 of g01.dev-tim.txt moves
#        full_p99_ms 13.903 -> 7.147 with rc 0 and judge_complete=1, and
#        the output still passes all three consumers' 23-key whitelists
#        unchanged (check-device-fullgame.sh:1160-1162,
#        check-device-target.sh:1078-1080, check-device-foh.sh:1336-1340)
#        because every consumer discards the sim keys. Leg [1]'s
#        "p99 frame < 16.67 ms" certification is therefore forgeable by a
#        recording-side defect the judge cannot see.
#        CLOSURE BAR (PROCESS §11): this row flips only when the false
#        green AND both codex [M]s are fixed and BOTH seconds reach an
#        anchored VERDICT: GO — resolving the two codex [M]s alone is NOT
#        sufficient, and the byte-identity regression passing does NOT
#        offset it (the archived judgments were all replayed against
#        honest recordings).
#   pipeline/lib/manifest.js, pipeline/lib/verify-artifacts.js,
#   pipeline/lib/check-expected.js, pipeline/expected.json
#     -> .loop/review-116-plib-1.log (codex) VERDICT: NO-GO — four [H]
#        + four [M], and .loop/review-116-plib-2-grok.log (grok, the
#        independent second) VERDICT: NO-GO — two [H], INDEPENDENTLY
#        REPRODUCED: verify-artifacts.js passes vacuously on an empty
#        artifact set (live repro: `{"stages":{}}` prints
#        "0 artifact(s) OK", exit 0), and check-expected.js derives its
#        required contract FROM the file it is checking, so a deleted
#        expected.json section is simply never asserted. Codex adds that
#        non-audio artifact identity is self-referential (the M4 path
#        runs only that self-check) and that audio shape validation is
#        skippable/coercive. These are real fail-open holes in M1-gate
#        machinery; closing them changes the M1 contract and the M4
#        composition, which is its own task with its own arcs.
#
# port/gfx/judge-render-timing.js — why it is here:
#   - No VERDICT: GO has ever NAMED this file (iter-108 ledger §"Weakest
#     row", .loop/m4-t14-producer-status.md, which records that
#     `arc-pending` "would also be honest").
#   - Round 1 of this manifest's own arc ruled the inherited
#     `reviewed-go` UNSUPPORTABLE ([H], .loop/review-116-1.log): the
#     cited .loop/review-61-1.log is an audio arc scoped to
#     check-device-audio.sh + platform_audio_sdl.h (its own line 14), so
#     its GO does not approve this judge; and a task-4 cap that PREDATES
#     the iter-62 bytes cannot approve them.
#   - PROCESS §3 puts judge paths at Tier A+ (independent second
#     reviewer + byte-identity regression on archived results), which
#     this file has never had.
# Restoring `reviewed-go` requires that Tier A+ arc — NOT a re-cite.
# The M3 manifest keeps its own inherited status for this file; it is
# driver-owned and untouched here (m3 is ratified; this gate does not
# get to rewrite it, and does not inherit its approval either).
# --- UNIVERSE EXTENSION (iter 116, driver-sanctioned strengthen-only gate
# edit). Round 1 of this manifest's own arc (.loop/review-116-1.log,
# finding 1 [H]) measured EIGHT decision-bearing producers reachable from
# the gate's four engines that the frozen REQUIRED_PRODUCERS literal did
# not name: they were pinned only by the ENGINES ("the engine's own
# guarantee, not the gate's" — check-device-fullgame.sh:718-721 says so in
# terms and asks for exactly this), so they carried NO [0b] approval
# status and could not refuse before a leg. The driver reopened the frozen
# verify_m4.sh for the single purpose of ADDING them to the universe
# (strengthen-only: rows added to the refusal set, no grammar/refusal/
# sentinel arm relaxed). Reachability was re-measured for this change, not
# inherited; each row's status below is the honest outcome of its own
# evidence search, not a promotion.
#
# Settled by their own evidence (no new arc needed):
#   json-dup-key-scan.js — reviewed by .loop/review-98-1.log in full (its
#     prompt scope names it, the diff carries its hunk, and a finding
#     cites json-dup-key-scan.js:45); that log's LAST anchored verdict is
#     GO, and its arc covered commit a3fc74d = the file's current bytes.
#   keymap-frozen.txt — its content diff landed in the .loop/review-95-1
#     -> .loop/review-97-1 arc, which closed GO; BYTE-BOUND, not
#     mtime-bound: the current sha256 2b208cfe… appears verbatim in both
#     .loop/review-95-1.log and .loop/review-97-1.log, so the reviewed
#     bytes and the pinned bytes are provably the same bytes.
#   dump-sim-data.js — M2 task 17 (commit 24c59a7), untouched since,
#     proven by the M2 exit-gate corpus exactly as trace-to-txt.js is;
#     same status token, same basis, no stronger claim made.
#   streamlib.js — oracle/ HARD RULE 3 surface, M0 task 5 (commit
#     b32e06c), untouched since freeze; identical basis to the
#     verify-stream.js row that already carries oracle-frozen (it is the
#     module verify-stream.js requires).
#
# Opened as their own Tier A+ arcs (PROCESS §3: verification/comparison
# logic), because the evidence search found NO arc that ever reviewed
# their content — every log naming them ends NO-GO, and the GO logs that
# mention them do so only as pin rows or as call sites inside another
# file's diff. See the UNCLOSED ROWS block.
#
# --- UNIVERSE EXTENSION #2 (iter 117, driver ruling 3, strengthen-only).
# The FROZEN COMPARISON BASIS of leg [1] was unpinned. Leg [1] judges every
# M4 golden's device stream against port/goldens-m4/*.sha256.json using the
# UNCHANGED oracle verify-stream.js — but nothing in the gate pinned those
# frozen files or the two manifests that select and parameterise them, so
# the standard against which conformance is measured could drift while the
# gate reported conformance. (oracle/goldens/ needs no such row: HARD RULE
# 3 makes it read-only to every task after M0, and the gate's own engines
# re-derive nothing from it.) Ten rows added: both manifests + all eight
# frozen streams (the four match/scenario streams, the two target streams,
# and the two .target.sha256.json target-plane streams). The .trace.json
# files are NOT separate rows and do not need to be: each frozen stream
# pins its trace's sha256 and verify-stream.js asserts it, so a swapped
# trace fails inside an already-pinned file.
# Status basis, verified row by row rather than assumed:
#   - m01/m02/s01/s02 + manifest.json — the M4-golden RECORDING machinery
#     (record-m4.sh, freeze-stream-m4.js, check-quality.js) closed its arc
#     at .loop/review-86-1.log, LAST anchored verdict GO, and that log names
#     m01/m02/s01/s02 and record-m4.sh (x64) / freeze-stream-m4.js (x32).
#     m01, m02 and s01 are additionally BYTE-BOUND: each one's CURRENT
#     sha256 appears verbatim inside an earlier round of that same arc
#     (.loop/review-81-1.log for m01/m02, .loop/review-82-1.log and
#     .loop/review-84-1.log for s01), so the reviewed bytes and the pinned
#     bytes are provably the same bytes. s02 and manifest.json are bound by
#     the arc naming them plus NO commit touching them since (56415ab).
#   - t01/t02 (both stream and target-plane stream) + manifest-target.json —
#     the TARGET rig arc closed at .loop/review-98-1.log, LAST anchored
#     verdict GO, over commit a3fc74d; that round's own scope names
#     manifest-target.json (x24), t02 (x20) and .target.sha256 (x25), and
#     its dup-key scanner was run over every port/goldens-m4/*.json. No
#     commit has touched these files since 927294e.
# What is NOT claimed: these are RECORDED EVIDENCE, and the claim is that
# reviewed machinery produced them and that the reviewed bytes are the
# pinned bytes — not that a reviewer independently re-derived a checksum
# stream by hand. Re-recording is the mechanical check (record-m4.sh must
# print `unchanged (byte-identical re-freeze)`); a DIFFERING overwrite
# needs --refreeze and a spec version bump.
#
# --- RULING 4 (iter 117): the false "pinned judge" comment, and why the
# three skip-attrib diagnostics did NOT become rows.
# check-device-fullgame.sh's attribution arm called correlate-skips.js
# "the UNCHANGED pinned judge". It was not pinned. The driver's ruling was
# to correct the comment AND pin correlate-skips.js / validate-ev.js /
# sk_sampler.c "with statuses from their genuine arc history — verify".
# VERIFIED, and the premise does not hold:
#   - NO VERDICT: GO has ever covered any of the three. Their arcs are
#     .loop/review-{73,76,78}-1.log with diff packets
#     .loop/review-{73-74,76,78}-diff.txt — every one closed NO-GO. The
#     only log carrying correlate-skips.js's current sha256 is the NO-GO
#     .loop/review-78-1.log; sk_sampler.c's and validate-ev.js's current
#     shas appear in no log at all. The later GO logs that MENTION them
#     (110-5, 111-3, 114-5, 106-4, 89-1) had other files in scope.
#   - validate-ev.js is not reachable from the gate at all: its sole
#     caller repo-wide is port/sim/device/check-skip-attrib.sh, which is
#     not a gate engine.
#   - correlate-skips.js is reachable only through the ARMED arm, and the
#     gate never arms it: check-device-fullgame.sh:153 reads
#     ATTRIB="${MLFK_FULLGAME_ATTRIB:-0}" and verify_m4.sh sets no such
#     variable, so ATTRIB is 0 on every gate run. An armed run is
#     structurally non-authoritative anyway (its ` [ATTRIB-ARMED]` suffix
#     cannot match the anchored FULLGAME_RE).
#   - sk_sampler.c is compiled by the shared arm build (riglib.sh:1725)
#     but is never run by any gate leg and emits no verdict.
# Pinning them would therefore have added unapproved rows that REFUSE the
# gate over evidence no gate leg consumes — manufacturing a status to
# avoid that is precisely the laundering round 1 raised as [H]2. The
# honest half of the ruling was executed instead: the comment now states
# what is true, and cites the measurements above. ESCALATED TO DRIVER as
# an explicit deviation from ruling 4's second half, with the evidence.
7b031055e2329269e4e207012202cd772a75b604bd37ab3df8de9db325e4a254 port/sim/device/adbsh.sh reviewed-go task-4-arc-CAPPED-CLOSED-driver-accepted-AGENT-LOG-2026-07-17+rounds-.loop/review-52-1.log-.loop/review-55-1.log
0c9126aa1789ad575a76f4e725b231a64cd3d792397aefc9a47dc39801213752 port/sim/device/riglib.sh reviewed-go .loop/review-109-9.log-capped-driver-accepted-AGENT-LOG-iter-109+delta-GOs-.loop/review-110-5.log-.loop/review-113-7.log
4034ad81601de4e25b85658095ebfd38de2cc813fad594164afb563d6deb81ad port/sim/device/percentiles.js reviewed-go .loop/review-45-1.log+r3-closure-.loop/review-47-1.log
84a8b2ca249632b758083e998cc39bcf53101e2782113314797efb9f92fcf97e port/sim/device/check-device-fullgame.sh arc-in-flight iter-117-ruling-4-comment-correction-bytes-in-the-cumulative-delta-arc+prior-bytes-.loop/review-113-7.log-r7-GO-final-bytes+iter-109-arc-capped-driver-accepted-AGENT-LOG-iter-109+delta-GOs-.loop/review-110-5.log-.loop/review-111-4.log-.loop/review-112-5.log
d0f8f031f576d8d2bf4fa106a18a42284a44e42be6205588d1dd38b952dd9235 port/sim/target/check-device-target.sh arc-in-flight iter-117-jrt-producer-pin-fanout-line-119-only-in-the-jrt-arc-per-.loop/review-117-jrt-6o.log-H1+prior-bytes-.loop/review-101-1.log+r4-closure-.loop/review-105-1.log
415335239fcc04df97eba07298a1fa521602d5ea45b087aa8d7d40bd740c122a port/goldens-m4/verify-target-stream.js reviewed-go .loop/review-98-1.log
2cf5c5a532207372b70c4cee57412c7ac65643ac4f4066c745d9eb7fe4aa0e9b port/goldens-m4/wrap-target.js reviewed-go .loop/review-98-1.log
6b1b6b5be3700c51dfae8c0c4cb1f012e5b61239394ae4146c2e5e19cc4fcc47 port/goldens-m4/validate-target-manifest.js reviewed-go .loop/review-98-1.log
624956898890e749170a4768af0f8ef86e05ce4dd75046d084701747c9d9121f port/goldens-m4/json-dup-key-scan.js reviewed-go .loop/review-98-1.log-full-review-of-commit-a3fc74d-current-bytes
f10e00010c769642e550959f3e3ea329bb6c0ae32ca642c3a8a0e29a69135aac port/goldens-m4/manifest.json reviewed-go .loop/review-86-1.log-GO-closes-the-record-m4.sh+freeze-stream-m4.js-arc-that-registered-m01-m02-s01-s02+rounds-.loop/review-81-1.log-.loop/review-83-1.log-.loop/review-84-1.log
b1fe39e64b8e76b8b7e361e0b3c42258f8140d0caad58e5a1df941f5c714f1ac port/goldens-m4/manifest-target.json reviewed-go .loop/review-98-1.log-GO-target-rig-arc-closure-names-manifest-target.json-x24+rounds-.loop/review-94-1.log-.loop/review-96-2.log
a48c47b0bc682abbade75d38ec00832d4235f6f22dd7e5015eba516213688b6b port/goldens-m4/m01-falcon-marth-d1-ystory.sha256.json reviewed-go .loop/review-86-1.log-GO-arc-closure+BYTE-BOUND-current-sha-a48c47b0-appears-in-round-.loop/review-81-1.log
041bc10edcb333480f7d8390aa161861f2b80e8839e35fc1bbc700c247f5cab1 port/goldens-m4/m02-falcon-fox-d9-dreamland.sha256.json reviewed-go .loop/review-86-1.log-GO-arc-closure+BYTE-BOUND-current-sha-041bc10e-appears-in-round-.loop/review-81-1.log
4229cdebcbc18edb1f62812feb017fdfc24b87011b3af7159c674501cd5d9126 port/goldens-m4/s01-marth-fox-stops-battlefield.sha256.json reviewed-go .loop/review-86-1.log-GO-arc-closure+BYTE-BOUND-current-sha-4229cdeb-appears-in-rounds-.loop/review-82-1.log-.loop/review-84-1.log
67bfaee2c5df277dc1c6434516eea2a85cfec393421870e5343e707ab15ab076 port/goldens-m4/s02-marth-fox-guardon-break-battlefield.sha256.json reviewed-go .loop/review-86-1.log-GO-arc-closure-names-s02-golden+rounds-.loop/review-83-1.log-.loop/review-84-1.log-NO-commit-since-56415ab
5ae4a05d161d2d6d4826828cc0c8c6b32bbdf778f9e09457e1b7e1a1f949ca95 port/goldens-m4/t01-fox-lasers-tstage1.sha256.json reviewed-go .loop/review-98-1.log-GO-target-rig-arc-closure-scanned-every-goldens-m4-json+round-.loop/review-94-1.log-NO-commit-since-927294e
677c81e677ece8dc2442ab340a7227bfb4ce44d416348d357327db76f4032daf port/goldens-m4/t01-fox-lasers-tstage1.target.sha256.json reviewed-go .loop/review-98-1.log-GO-target-rig-arc-closure-scanned-every-goldens-m4-json+round-.loop/review-94-1.log-NO-commit-since-927294e
6e4116060b6e81fa57313c6fe08544201edfa23a7eb4d8b5f2e0362f3e906afb port/goldens-m4/t02-falcon-melee-tstage2.sha256.json reviewed-go .loop/review-98-1.log-GO-target-rig-arc-closure-scanned-every-goldens-m4-json+round-.loop/review-94-1.log-NO-commit-since-927294e
d0718dd760ffa802326f2de42078bd8cc55de75440113fa0b7d34e676a757e30 port/goldens-m4/t02-falcon-melee-tstage2.target.sha256.json reviewed-go .loop/review-98-1.log-GO-target-rig-arc-closure-scanned-every-goldens-m4-json+round-.loop/review-94-1.log-NO-commit-since-927294e
d2f742ba006d271fa807bc3bfd1113543218af07a928a48a049ee86a39f599ea port/foh/check-device-foh.sh arc-in-flight iter-117-jrt-producer-pin-fanout-line-142-only-in-the-jrt-arc-per-.loop/review-117-jrt-6o.log-H1+prior-bytes-.loop/review-97-1.log+iter101-delta-in-.loop/review-101-1.log+r4-closure-.loop/review-105-1.log
2267f8b796b1881d6ef749b5931a5fb08ae9f914b7a67a0e2608d4cada99616e port/foh/judge-foh-trace.js reviewed-go .loop/review-101-1.log+r4-closure-.loop/review-105-1.log
b2e814821a809a6cb5772c51587b1e49a87dab6438156f984e8ec07e59ab2141 port/foh/normalize-foh-trace.js reviewed-go .loop/review-99-1.log+r4-closure-.loop/review-105-1.log
b0c6ac328d6b87f9ff0c58562c880336eccb98a3053a4fdaadfb71e59e810186 port/foh/flow-to-fkscript.js reviewed-go .loop/review-95-1.log+r3-closure-.loop/review-97-1.log
2b208cfe18c9e5aac370e0212fc74721489fd404aeb67c9deeddee88ba1bfc1e port/foh/keymap-frozen.txt reviewed-go .loop/review-95-1.log+r3-closure-.loop/review-97-1.log-BYTE-BOUND-current-sha-appears-in-both-logs
de32dffbe43423bee43f67516007d4039abbd80237a626aec81e54fd7dc4ca68 port/gfx/judge-shot.js reviewed-go .loop/review-73-1.log+r4-closure-.loop/review-80-1.log
4b68fba5a804b281a73003b29eac1a0290707f2b6260ee39c900a0262962f421 port/gfx/judge-render-timing.js reviewed-go iter-117-Tier-A-plus-arc-CLOSED-VERDICT-GO-.loop/review-117-jrt-9b.log+independent-second-.loop/review-117-jrt-8o.log+byte-identity-regression-100-of-100-.loop/review-117-jrt-regression-r7.log-R12
8ceaf028fe5740a1c0bb384e2eae113996bdb5c3db5720f1337af068266b966f port/gfx/judge-audio-summary.js reviewed-go .loop/review-59-1.log+r3-.loop/review-61-1.log
bf29fa7cba83708cfff093195f48ad20a8eda2ad794fe25b270f211b08876eee port/gfx/pack-snd.js reviewed-go .loop/review-57-1.log+r2r3-closure-.loop/review-61-1.log
d43628d4a22e278ebffa8e3116709cff52933332a51a926fb28c3cfa4ad0dbc4 port/gfx/check-device-opk.sh reviewed-go .loop/review-115-3.log-r3-GO+r1-.loop/review-115-1.log+r2-.loop/review-115-2.log
d761707065185bf49078c44c83a111558b6bebfbf7dba3e8625eff5951385dec port/gfx/opk/mlfk-foh.sh reviewed-go .loop/review-93-1.log+r3-closure-.loop/review-97-1.log
3b4289d640bbe7df0e4d065d909f2b46aef9b7e4d7622a90e4f7021fd801dbea port/gfx/opk/meleelight-foh.funkey-s.desktop reviewed-go .loop/review-115-3.log-r3-GO-current-bytes+prior-arc-.loop/review-93-1.log+r3-closure-.loop/review-97-1.log
efea406086050d51b3894692c6030afef1b0cc7ed06de442b728ef2e6072fe99 port/gfx/opk/icon32.png reviewed-go .loop/review-58-1.log+r2-.loop/review-60-1.log+gate-closure-.loop/review-62-grok-go.log
05ced80f57e993541f6da8b647e63abcac4163cd6e5b275f2a811a49c2faeb76 port/tools/fk_input.c reviewed-go .loop/review-53-1.log
b835b5f886225e0015dae152576eea5a42fa69d7ba0699f4de0e31438d05c5b9 port/sim/sim/wrap-run.js reviewed-go .loop/review-81-1.log+r3-closure-.loop/review-86-1.log
4160a35b36e8d3d6896ad2c3c6239d4a4860a0d7f43814a7a9b53b7c136742ab port/sim/sim/trace-to-txt.js grandfathered-m2 M2-exit-gate-SIM-CONFORMS
7186734f8c3ff9bfad04f59bf9e13f201663e82481e399911433136673721bba port/sim/calib/dump-sim-data.js grandfathered-m2 M2-exit-gate-SIM-CONFORMS
f420723433b19166b53a80aedf54931ffdfbc6d2505c773fd73b7a13bbcdf60e oracle/harness/verify-stream.js oracle-frozen M0-task5-HARD-RULE-3
0bc801ea46b06a63e79377aae164636a5e9f649ee45835748e5f2387b9e04281 oracle/harness/streamlib.js oracle-frozen M0-task5-HARD-RULE-3
46606bed441ae2923c7c67355f12b789f5854543295c5e718fa679bf23a0d533 pipeline/lib/manifest.js reviewed-go .loop/review-117-plib-4o.log-GO-closure-round+.loop/review-117-plib-2o.log-GO+NO-GO-rounds-.loop/review-116-plib-1.log-.loop/review-116-plib-2-grok.log-.loop/review-117-plib-1.log+PROCESS-11-Opus-5-second-codex-aborted-x3-cyberfilter-grok-x3-no-verdict
826e16854bdbfb6061052d1a0dbdcc4675f282754b04fb5262f34b5b30283a45 pipeline/lib/verify-artifacts.js reviewed-go .loop/review-117-plib-4o.log-GO-closure-round+.loop/review-117-plib-2o.log-GO+NO-GO-rounds-.loop/review-116-plib-1.log-.loop/review-116-plib-2-grok.log-.loop/review-117-plib-1.log+PROCESS-11-Opus-5-second-codex-aborted-x3-cyberfilter-grok-x3-no-verdict
ec578b42d0490448d61bef3f21c958105a48d99c4cc1a5d403a742e7627b8e18 pipeline/lib/check-expected.js reviewed-go .loop/review-117-plib-4o.log-GO-closure-round+.loop/review-117-plib-2o.log-GO+NO-GO-rounds-.loop/review-116-plib-1.log-.loop/review-116-plib-2-grok.log-.loop/review-117-plib-1.log+PROCESS-11-Opus-5-second-codex-aborted-x3-cyberfilter-grok-x3-no-verdict
31e5946a0269095f7895b01aaf7f78e9c3496aae75c638b8cd1b4a678c4bd29b pipeline/expected.json reviewed-go .loop/review-117-plib-4o.log-GO-closure-round+.loop/review-117-plib-2o.log-GO+NO-GO-rounds-.loop/review-116-plib-1.log-.loop/review-116-plib-2-grok.log-.loop/review-117-plib-1.log+PROCESS-11-Opus-5-second-codex-aborted-x3-cyberfilter-grok-x3-no-verdict
a574fec40685b6770e85d55ee1aaabd35553caab00c9aeadec0ea234b4173590 pipeline/lib/tables-anim-xref.js grandfathered-m2 M1-era-checker-unchanged-since-iter10-21da425-proven-by-the-M1-EXIT-GATE-PIPELINE-OK+newly-pinned-per-.loop/review-117-jrt-8.log-H+STATUS-TOKEN-VOCABULARY-STRETCH-M1-NOT-M2-FLAGGED-FOR-DRIVER
7d54e55ab3d0666ae2f46b4efa7e0b4aa0e158a9349276468dac9c67e48b73e6 port/sim/device/verify_m4.sh arc-in-flight iter-116-universe-extension-bytes-UNREVIEWED-delta-arc-.loop/review-116-2o.log-NO-GO+codex-round-.loop/review-116-2-cached-DISCARDED.log-cmp-proven-byte-identical-to-.loop/review-116-plib-1.log+prior-bytes-only-.loop/review-108-4.log-r4-GO
