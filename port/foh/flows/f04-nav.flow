FLOW1
# f04: navigation + refusal + destination coverage (no launch).
#
# REFUSALS. Two of the four still-emitted tokens fire here:
# targetbuilder (menu-top) and random (sss — iter 93, the registered
# seeded-draw exclusion). addcode fires in f07's
# target-select leg; portconfig in the check's own T19 flow. RETIRED as
# emissions this arc: `audio` (a real screen now, MENU-SPEC §4) and
# `controller`/`keyboard` (real destinations, §9) — this flow now ENTERS
# the HANDHELD destination instead of being refused by it (the CONTROLLER
# one is unreachable at FOH_CTL_CHOOSER 0, see below). UNREACHABLE
# while FOH_NETPLAY is 0 (owner ruling C5): spectate/p2p/server, whose
# page `VS. Melee` no longer opens.
#
# A7 (MENU-SPEC §8) RETIRES `credits` THE SAME WAY, and this flow follows
# the precedent the paragraph above set for `audio`: the CREDITS row is a
# real destination now (gameMode 13, menu.js:145-149), so the leg at 480
# ENTERS it and leaves by its own B (credits.js:236-245), which lands back
# on Options with the cursor still on CREDITS. The menu-level B that follows
# it still reaches menu-top from row 3. No SHOT is taken
# there: the shot inventory is pinned per flow in check-foh-flows.sh and
# A7 does not move it. The screen's PIXELS are asserted by
# port/foh/check-credits.sh instead, which is where its own gestures live.
#
# THE CONTROLS LEG (DEVIATION D27, owner ruling 2026-08-23; the switch is
# foh.h's FOH_CTL_CHOOSER and this flow is frozen at 0). The chooser is
# COLLAPSED: A on the Options CONTROLS row lands on the HANDHELD screen
# (controls-keyboard, gameMode 12) directly, and its B returns to Options
# with the cursor still on the row that opened it — which is why ONE DOWN,
# not three, now reaches CREDITS at frame 475. The chooser page
# (menu-controls) and the CONTROLLER destination (controls-controller,
# gameMode 14) are UNREACHABLE at that flag, exactly as menu-battle is at
# FOH_NETPLAY 0, so this flow can no longer shoot them and does not pretend
# to. D25's labels and row order are untouched by the collapse; they are
# asserted by port/foh/check-controls-labels.sh, at BOTH flag values.
#
# EDGES. Every B-back edge of the live graph fires: controls-keyboard ->
# menu-options, menu-options -> menu-top, plus the CSS hold-B-30 back edge
# (css.js:186-191), which under C5 lands on menu-top with the cursor on
# VS. Melee. The css -> sss (START, css.js:446-451) and sss -> css (B,
# stageselect.js:79) pair is traversed before it. The U-wrap witness is
# the three UPs from OPTIONS(3) back to VS. MELEE(0).
I 1 -
I 375 S
I 376 -
I 380 D
I 383 -
I 385 D
I 388 -
I 390 A
I 391 -
I 395 D
I 398 -
I 400 A
I 401 -
I 405 D
I 408 -
I 410 D
I 413 -
I 415 A
I 416 -
SHOT 420 controls-keyboard
I 425 B
I 426 -
I 475 D
I 478 -
I 480 A
I 481 -
# A7: 480 enters the credits (gameMode 13) and 483 leaves by its own B.
# Three frames on the screen is deliberate — long enough for one full tick
# of its machine, short enough that the 2500-frame timer exit (Quirk Q6)
# stays out of this flow entirely. The menu-level B that follows moved from
# 485 to 487 for a DEVICE reason, not a machine one: flow-to-fkscript.js
# refuses two presses of the same keysym less than three flow frames apart
# (the injector would emit a down edge with no release between), and the
# credits exit is also a B. It still lands on menu-top from row 3, because
# credits.js leaves menuMode/menuSelected alone.
I 483 B
I 484 -
I 487 B
I 488 -
I 490 U
I 493 -
I 495 U
I 498 -
I 500 U
I 503 -
I 505 A
I 506 -
# CSS mechanics arc (docs/MENU-SPEC.md item 4): START launches ONLY while
# READY TO FIGHT, and the CSS opens with one participant (P1 joined at
# the title; playerType init is [-1,-1,-1,-1], main.js:107). So this leg
# must make a second port non-N/A before it can cross to the SSS.
# Clamp-anchored like f01: LEFT+DOWN x105 pins the hand at (0, 240), then
# RIGHT x33 -> x = 79.20 and UP x36 -> y = 101.76 puts it on port 2's
# type tab. The hand never enters the roster band on that path, so
# nothing here can touch a token.
I 510 LD
I 615 -
I 620 R
I 653 -
I 658 U
I 694 -
I 700 A
I 703 -
I 710 S
I 713 -
I 720 D
I 723 -
I 730 D
I 733 -
I 740 A
I 743 -
I 750 B
I 753 -
# The B-hold back edge (css.js:186-194) fires from OUTSIDE the roster
# band, so it exercises the hold counter alone — the deliberate overlap
# with the B-grab (MENU-SPEC §2.11) is f01's territory. Held 40 frames,
# not exactly 30: the counter fires on the equality `bHold == 30`
# (css.js:188), so the extra frames are slack for the device injector's
# +/-1 frame and change nothing about when it fires.
I 760 B
I 800 -
END 810
